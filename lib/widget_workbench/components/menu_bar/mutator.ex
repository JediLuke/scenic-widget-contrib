defmodule WidgetWorkbench.Components.MenuBar.Mutator do
  @moduledoc """
  State mutation logic for the MenuBar component.
  Reserved for future complex state transformations.
  """
  
  # Currently, state mutations are handled directly in the Reducer
  # This module is here for consistency with the 6-file pattern
end